
//TODO: 
//      add more objects to worldbuilder
//      sort objects and show/hide them depending which layer is selected
//      add win condition
//          collect objects (array of objects to collect)
//          return to spaceship after collecting all objects
//      add more block types
//          spring
//          ice
//          coin (after win condition added)
//          spike (after loss condition added)
//      add save function to worldbuilder
//      add reverse animations to player
//      add bottom collision to objects
//      add fun down animation
//      general animation function to loop through all objects that need to animate





var fs = require('fs');
var stream = fs.createWriteStream("my_file.txt");
stream.once('open', function(fd) {
  stream.write("My first row\n");
  stream.write("My second row\n");
  stream.end();
});
